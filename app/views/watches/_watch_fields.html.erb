<%= f.fields_for :watch do |g| %>
  <div class="row">
    <div class="form-group col-sm-4">
      <%= g.label :brand %>
      <%= g.text_field :brand, class: 'form-control' %>
    </div>
    <div class="form-group col-sm-4">
      <%= g.label :model %>
      <%= g.text_field :model, class: 'form-control' %>
    </div>
  </div>
  <div class="row">
    <div class="form-group col-sm-4">
      <%= g.label :condition %>
      <%= g.text_field :condition, class: 'form-control' %>
    </div>
    <div class="form-group col-sm-4">
      <%= g.label :price %>
      <%= g.text_field :price, class: 'form-control' %>
    </div>
  </div>
<% end %>
