<%= f.fields_for :watch do |g| %>
  <div class="row">
    <div class="form-group col-sm-4">
      <%= g.label :Primary_choice, class: 'form-label' %>
      <%= g.text_field :primary, class: 'form-control' %>
    </div>
    <div class="form-group col-sm-4">
      <%= g.label :Secondary, class: 'form-label' %>
      <%= g.text_field :secondary, class: 'form-control' %>
    </div>
  </div>
å
<% end %>
